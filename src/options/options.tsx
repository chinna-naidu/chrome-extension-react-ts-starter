import React from "react";
import ReactDOM from "react-dom";

export default function Options() {
  return <div>options works!!!</div>;
}

ReactDOM.render(<Options />, document.getElementById("root"));
